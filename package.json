{
  "name": "codex-quota-widget",
  "version": "0.1.0",
  "description": "Tiny bridge and Scriptable widget for Codex quota.",
  "type": "module",
  "bin": {
    "codex-quota-widget": "./server.mjs"
  },
  "scripts": {
    "check": "node --check server.mjs && node --check scriptable-widget.js && node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
